variables:
  container_name: "combinedIngress"
  dockerhub_repo: "mozorg/combinedIngress"


docker-build:
  image: docker:latest
  stage: build
  services:
    - docker:dind
#  before_script:
#    - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" $CI_REGISTRY
  script:
    - docker build --pull -t "$container_name:`cat VERSION`" .
    - docker push "$dockerhub_repo"
#  only:
#    - master